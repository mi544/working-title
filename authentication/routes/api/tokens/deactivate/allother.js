const router = require("express").Router();

router.route("/").get((req, res) => {
    // get a token and a userId
    // deactivate all tokens for this user but this token
});

module.exports = router;
